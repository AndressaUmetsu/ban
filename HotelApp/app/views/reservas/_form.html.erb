<%= form_for(reserva) do |f| %>
  <% if reserva.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reserva.errors.count, "error") %> prohibited this reserva from being saved:</h2>

      <ul>
      <% reserva.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_reserva %>
    <%= f.date_select :data_reserva, class: 'form-control'  %>
  </div>

  <div class="field">
    <%= f.label :data_checkin %>
    <%= f.date_select :data_checkin, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :data_checkout %>
    <%= f.date_select :data_checkout, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :valor_entrada %>
    <%= f.text_field :valor_entrada, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :cliente_id %>
    <%= f.text_field :cliente_id, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :quarto_id %>
    <%= f.text_field :quarto_id, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :hotel_id %>
    <%= f.text_field :hotel_id, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
