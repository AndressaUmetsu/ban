<%= form_for(tipo_quarto) do |f| %>
  <% if tipo_quarto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_quarto.errors.count, "error") %> prohibited this tipo_quarto from being saved:</h2>

      <ul>
      <% tipo_quarto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome, class: 'form-control'  %>
  </div>

  <div class="field">
    <%= f.label :preco %>
    <%= f.text_field :preco, class: 'form-control'  %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
